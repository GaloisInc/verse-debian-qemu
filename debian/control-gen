#! /bin/sh
set -e

if dpkg-vendor --derives-from Ubuntu
then tag=ubuntu
else tag=debian
fi

sed \
 -e "1i# autogenerated file: please edit debian/control-in" \
 -e "s/^:$tag://" \
 -e "/^:[a-z]*:/D" \
 debian/control-in > debian/control.tmp

mv -f debian/control.tmp debian/control
